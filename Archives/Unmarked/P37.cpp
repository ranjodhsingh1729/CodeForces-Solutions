#include <bits/stdc++.h>

#define MAXLEN 100000

using namespace std;

int main() {
    int t;
    cin >> t;

    int n, m;
    while (t--) {
        cin >> n >> m;

        cout << fixed;
        cout << n * (m / 2) << '\n';     
    }        

}